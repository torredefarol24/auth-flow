import { Request, Response, NextFunction } from 'express'

export const checkUserJWT = async (request: Request, response: Response, next: NextFunction) => {

      // Implement User JWT Checking Later
      next()

}
